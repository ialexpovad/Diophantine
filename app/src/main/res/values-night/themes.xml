<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Colors -->
    <color name="colorPrimary">#5387ED</color>
    <color name="colorPrimaryDark">#000000</color>
    <color name="colorAccent">#5387ED</color>

    <!-- Toolbar Title Style -->
    <style name="ToolbarTitleStyle" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="ButtonTextStyle" parent="Widget.AppCompat.Button">
        <item name="android:textColor">@android:color/white</item>
    </style>
    <style name="CheckBoxTextStyle" parent="Widget.AppCompat.CompoundButton.CheckBox">
        <item name="android:textColor">@android:color/white</item>
    </style>


    <!-- AppBar Overlay Style -->
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <!-- Popup Overlay Style -->
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Dark" />


    <!-- Base application theme. -->
    <style name="Base.Theme.Diophantine.Dark" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="buttonStyle">@style/ButtonTextStyle</item>
    </style>


</resources>
